{
	"type": "module",
	"scripts": {
		"dev": "node ./build.js dev",
		"buildAndZip": "node ./build.js pack && zip -r h2l-extension-v0.2.zip crx",
		"tsc": "tsc --project ./src/window/tsconfig.json && tsc --project ./src/background/tsconfig.json"
	},
	"devDependencies": {
		"@types/chrome": "^0.0.241",
		"esbuild": "^0.18.14",
		"preact": "^10.16.0",
		"typescript": "^5.1.6",
		"water.css": "^2.1.1"
	}
}
